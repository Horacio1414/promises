import ItemList from '../ItemList/ItemList'
import App from '../../App';
import Mercedes from '../../imagenes/mercedes.jpg'

const Item = (props) => 
<>

<li>{props.id} {props.title} {props.description} {props.price}</li>
 
 
                        </>
export default Item;